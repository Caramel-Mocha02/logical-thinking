# ロジックツリートレーニングアプリ

論理的思考・構造化思考・問題分解力を鍛えるためのロジックツリー作成トレーニングアプリ。
ユーザーが1つのお題に対して自分でロジックツリーを作成し、AIがヒントや評価を返す（答えは教えない）。

開発者は初心者。シンプルで理解しやすく、保守しやすいコードを最優先する。

## 技術スタック

- フロントエンド: React + Vite + JavaScript（TypeScriptは使わない）
- UI: Material UI（Tailwindは使わない）
- ツリー描画: React Flow
- バックエンド: Node.js + Express
- DB・認証: Supabase（PostgreSQL + Supabase Auth）
- ORM: 使わない（Prismaは使わない。Supabaseクライアントを直接使う）
- AI: OpenAI API

## コーディング方針

- 複雑な実装より、シンプルで読みやすい実装を優先する
- 過剰な抽象化・共通化はしない。似たコードが数行あっても無理に共通化しない
- 将来の拡張を見越した作り込みはしない（今必要なものだけ作る）
- コメントは「なぜそうしたか」が非自明なときだけ書く。「何をしているか」は書かない
- 初心者にも分かるよう、重要な処理は実装前に簡潔に説明する

## 開発の進め方（フェーズ制）

指示書に定義された Phase 1〜14 の順番で実装する。
1つのフェーズが完成・動作確認できてから次のフェーズに進む。1度に大量のコードを書かない。

各フェーズの実装前に、以下を簡潔に説明してから着手する。

1. 何を作るか
2. なぜ必要なのか
3. どのファイルを変更するか

### 進捗

- [x] Phase 1: React + Vite環境構築
- [x] Phase 2: Material UI導入
- [ ] Phase 3: React Flow導入
- [ ] Phase 4: 空のロジックツリー表示
- [ ] Phase 5: ノード追加・編集・削除
- [ ] Phase 6: ノード移動・親変更
- [ ] Phase 7: Supabase Auth
- [ ] Phase 8: ツリー保存
- [ ] Phase 9: お題管理
- [ ] Phase 10: OpenAI APIによる評価
- [ ] Phase 11: ヒント機能
- [ ] Phase 12: 部分評価
- [ ] Phase 13: 評価履歴
- [ ] Phase 14: UI改善

## AIの評価・ヒントに関する重要なルール

- ユーザーの代わりに答えを完成させない（例: 「広告費を増やせばいい」のような答えの提示は禁止）
- 代わりに「なぜ？」「根拠は？」「もう一段分解できないか？」など、考える方向性を示す問いかけを返す
- ヒント機能でも、具体的な答えそのものは提示しない。考える観点（切り口）だけを提示する

## Git運用ルール

- コードを変更したら、その都度コミットする（意味のある単位でまとめる）
- コミットしたら、その都度 `git push` してGitHubリモートに反映する
- コミットメッセージは日本語で、「何を」より「なぜ」を意識して書く
- force push・履歴の書き換え（rebase -i, commit --amend 済みコミット等）はしない
- pre-commit等のフックはスキップしない（--no-verifyを使わない）
- 破壊的なgit操作（reset --hard, checkout -- 等）を行う前は、必ず `git status` で作業状態を確認する

## 環境変数

OpenAI APIキー、Supabaseの接続情報などの秘密情報は `.env` に置き、コミットしない
（`.gitignore` に `.env` を含める）。
